{% extends 'base.html.twig' %}

{% block title %}Inscription
{% endblock %}

{% block body %}
	<section class="hero-section">
		{% include 'components/_hero.html.twig' with {
			'title': 'Rejoignez la communauté Yu-Gi-Oh',
			'buttonText': 'Découvrir les avantages',
			'buttonLink': '#'
		} %}
	</section>

	<section class="container my-5 p-5 d-flex flex-column">
		<h2 class="mb-4 fw-bold text-center">Créer un compte</h2>
		{{ form_errors(registrationForm) }}

		{{ form_start(registrationForm) }}
		{{ form_widget(registrationForm) }}
		<button type="submit" class="btn btn-primary">S'enregistrer</button>
		{{ form_end(registrationForm) }}
	</section>
{% endblock %}
